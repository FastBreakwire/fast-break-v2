{
  "name": "fastbreak-website",
  "version": "2.0.0",
  "private": true,
  "description": "Fast Break sports news website (V2). Mobile app lives in a separate repo.",
  "scripts": {
    "api": "node server/api.js",
    "scout": "node scout/run.js",
    "check": "node --check server/api.js && node --check server/models.js && node --check server/highlightlyProvider.js && node --check data/stories.js && node --check data/sports.js && node --check scout/run.js && node --check scout/sources.js && for f in scout/lib/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": { "node": ">=18" },
  "license": "UNLICENSED"
}
